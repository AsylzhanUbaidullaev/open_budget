import 'package:flutter/material.dart';

class LoadinView extends StatelessWidget {
  const LoadinView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
